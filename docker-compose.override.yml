services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.nuxt
    environment:
      NUXT_HOST: "0.0.0.0"
      NUXT_PUBLIC_UPLOAD_URL: "http://localhost:8080"
      NUXT_PUBLIC_NOTIFICATION_URL: "http://localhost:8081"
      NUXT_PUBLIC_STORAGE_URL: "http://localhost:9000"
   
